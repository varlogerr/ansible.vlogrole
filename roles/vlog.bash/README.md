Configure bash

Usage
```
---
# ...
  roles:
  - role: vlog.bash
    bash:
      completion: true # install bash completion, optional. defaults to false
      prompt: true # install custom prompt from templates, optional. defaults to false
# ...
```
